
  # MikroTik Hotspot Manager PRD

A professional hotspot management system for MikroTik routers. Backend can be configured to use a VPS-hosted PostgreSQL database (see DEPLOYMENT.md).

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  